#!/bin/sh /etc/rc.common

START=99
STOP=10

start() {
    local script

    for script in /usr/local/bin/macvlan_*.sh; do
        [ -x "$script" ] || continue
        "$script" || logger -t macvlan-bridge "failed to apply $script"
    done
}

stop() {
    return 0
}
